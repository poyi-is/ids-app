# Ionic Design System — Internal Builds

This public repository hosts internal macOS test builds and the Tauri updater manifest.

These builds are ad-hoc signed and are not Apple-notarized. macOS may require testers
to approve the first launch manually. They are not intended for public production use.

The updater archive is authenticated with the Tauri updater signature published in
`latest.json`.
